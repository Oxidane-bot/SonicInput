# v0.7.7 - Fluent History UI polish

## 中文

### 亮点
**历史记录界面**
- 按最新 Fluent 设置窗口规范升级历史记录页面与录音详情面板。
- 修正历史列表在窄窗口下的文本溢出、状态列挤压和操作按钮拥挤问题。
- 历史列表区域现在随窗口高度自适应，不再固定高度导致内容被挤成一团。
- 详情面板改为 Fluent/QML 表面，保留复制、重试、删除和关闭操作。

**验证**
- 增加历史页正常、窄窗口、详情和空态的中英文视觉审计截图。
- 扩展 Fluent QML 回归测试，覆盖详情面板、删除刷新、列表高度和长文本布局。

### 发布产物
- `SonicInput-v0.7.7-win64.exe`
- 如本地模型目录可用，将同时提供 `SonicInput-v0.7.7-win64-offline.zip`

### 升级步骤
1. 下载 `SonicInput-v0.7.7-win64.exe`。
2. 关闭旧版本并替换可执行文件。
3. 配置和历史记录保持兼容。

---

## English

### Highlights
**History UI**
- Updated the History page and recording detail panel to match the latest Fluent settings UI.
- Fixed narrow-window overflow, crowded status/action columns, and long-text pressure in history rows.
- The history list frame now grows with the window height instead of staying fixed and compressing the page.
- The detail panel now uses a Fluent/QML surface while preserving copy, retry, delete, and close actions.

**Validation**
- Added bilingual visual-audit screenshots for normal, narrow, detail, and empty History states.
- Expanded Fluent QML regression coverage for the detail panel, delete refresh, list height, and long-text layout.

### Artifacts
- `SonicInput-v0.7.7-win64.exe`
- `SonicInput-v0.7.7-win64-offline.zip` when a local model directory is available

### Upgrade Steps
1. Download `SonicInput-v0.7.7-win64.exe`.
2. Close the old version and replace the executable.
3. Existing configuration and history data remain compatible.
