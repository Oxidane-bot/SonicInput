# v0.7.5 - 悬浮窗置顶修复与发布收尾

## 中文

### 亮点
**录音悬浮窗**
- 修复悬浮窗显示后未稳定置顶的问题，窗口现在会在每次显示时重新确认顶置状态。
- 保持悬浮窗不抢焦点，避免打断当前输入。
- 补充回归测试，覆盖显示时的顶置与窗口层级行为。

**事件流转**
- 调整 AI 处理完成事件的状态推进方式，避免过早覆盖后续文本输入完成态。

**构建与发布**
- 版本号推进到 v0.7.5，并同步更新 README、构建说明和发布入口。

### 发布产物
- `SonicInput-v0.7.5-win64.exe`

### 升级步骤
1. 下载 `SonicInput-v0.7.5-win64.exe`。
2. 关闭旧版本并替换可执行文件。
3. 配置和历史记录保持兼容。

---

## English

### Highlights
**Recording overlay**
- Fixed the overlay so it reasserts topmost state every time it is shown.
- Kept the overlay from stealing focus so it does not interrupt the current input flow.
- Added regression coverage for topmost and window layering behavior during show.

**Event flow**
- Adjusted AI processing completion so it no longer overwrites the later text-input-completed transition too early.

**Build and release**
- Bumped the version to v0.7.5 and synced README, build notes, and release entry points.

### Artifact
- `SonicInput-v0.7.5-win64.exe`

### Upgrade Steps
1. Download `SonicInput-v0.7.5-win64.exe`.
2. Close the old version and replace the executable.
3. Existing configuration and history data remain compatible.
