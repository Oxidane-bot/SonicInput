# v0.7.2 - Fluent 桌面界面迁移

## 中文

### 亮点
**Fluent 设置页**
- 设置窗口迁移到 Qt Quick / QML FluentWinUI3。
- 恢复完整设置覆盖面，保留语言、快捷键、转写、AI、音频输入和历史等主要配置入口。
- 修复界面语言切换不会立即应用的问题，并统一中文 UI 字体观感。

**录音悬浮窗**
- 录音悬浮窗迁移到 Fluent QML。
- 重新设计为更紧凑的波形胶囊，支持拖动后保存位置。
- 停止录音按钮改为低存在感轻量控件，减少对录音状态的干扰。

**构建与测试**
- Nuitka 构建脚本加入 QML / Qt Quick 运行时文件。
- 增加 Fluent QML 加载、设置覆盖面、语言切换、悬浮窗拖动和桌面 UI e2e 测试。

### 发布产物
- `SonicInput-v0.7.2-win64.exe`

### 升级步骤
1. 下载 `SonicInput-v0.7.2-win64.exe`。
2. 关闭旧版本并替换可执行文件。
3. 配置和历史记录保持兼容。

---

## English

### Highlights
**Fluent settings window**
- Migrated the settings window to Qt Quick / QML FluentWinUI3.
- Restored full settings coverage for language, hotkeys, transcription, AI, audio/input, and history surfaces.
- Fixed runtime language application and aligned Chinese UI font rendering.

**Recording overlay**
- Migrated the recording overlay to Fluent QML.
- Redesigned it as a tighter waveform capsule with persisted drag position.
- Reworked the stop control into a lighter, lower-emphasis button.

**Build and tests**
- Added QML / Qt Quick runtime files to the Nuitka build.
- Added Fluent QML loading, settings parity, language switching, overlay drag, and desktop UI e2e coverage.

### Artifact
- `SonicInput-v0.7.2-win64.exe`

### Upgrade Steps
1. Download `SonicInput-v0.7.2-win64.exe`.
2. Close the old version and replace the executable.
3. Existing configuration and history data remain compatible.
