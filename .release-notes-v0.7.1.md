# v0.7.1 - 分块最终输出修复

## 中文

### 修复
**分块最终输出完整性**
- 修复 Chunked 模式下停止录音时，最终输出有时只包含最后一个分块的问题。
- 已完成的前置分块现在会和停止时仍在处理的尾部分块一起按顺序合并，避免前文丢失。

**构建稳定性**
- Nuitka 构建脚本支持非交互下载必要辅助工具，避免自动化构建卡在确认提示。

### 发布产物
- `SonicInput-v0.7.1-win64.exe`

### 升级步骤
1. 下载 `SonicInput-v0.7.1-win64.exe`。
2. 关闭旧版本并替换可执行文件。
3. 配置和历史记录保持兼容。

---

## English

### Fixes
**Complete final chunked output**
- Fixed an issue where Chunked mode could output only the last chunk after recording stops.
- Previously completed chunks are now merged with the final pending chunk in order, preventing earlier text from being dropped.

**Build reliability**
- Nuitka builds can now download required helper tools non-interactively, avoiding prompts in automated release builds.

### Artifact
- `SonicInput-v0.7.1-win64.exe`

### Upgrade Steps
1. Download `SonicInput-v0.7.1-win64.exe`.
2. Close the old version and replace the executable.
3. Existing configuration and history data remain compatible.
